import * as React from 'react';
import styled from 'styled-components';

const Feed: React.FC<{}> = () => {
  React.useEffect(() => {
    
  });

  return (
    <div>

    </div>
  );
}

export default Feed;
